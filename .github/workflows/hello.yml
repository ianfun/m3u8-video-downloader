on: push
jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: Dev
    steps:
      - uses: actions/checkout@v3
      - name: deploy
      - shell: bash
        env:
          SUPER_SECRET: ${{ secrets.Hello }}
        run: hello "$SUPER_SECRET"
